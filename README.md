# WF-07 recorded model output

These videos and their audio tracks were generated by MiniMaxAI/MiniMax-H3. They are example outputs, not ground-truth references or a visual-quality benchmark.

The run started on 2026-09-11 at 15:59:24 UTC. It used vLLM-Omni commit `33fff2155e0bf9bc8688f7ffb92ef508efdf6954` with the WF-07 changes and ComfyUI commit `cbbc9dab1f03d0d9a6caa8a8be7d77a7e37e1e44`. The submitted graph, prompt, seed and sampling parameters are in [submitted_prompt.json](submitted_prompt.json). The ComfyUI history reports an empty cached-node list for this run.

| Recorded field | Generated | Upscaled |
| --- | --- | --- |
| Video dimensions | 1344 × 768 | 2688 × 1536 |
| Frames / FPS | 124 / 24 | 124 / 24 |
| Video duration | 5.166667 s | 5.166667 s |
| Audio | AAC, 32 kHz, stereo | AAC, 32 kHz, stereo |
| Audio duration | 5.167 s | 5.167 s |

[generated.mp4](generated.mp4) is the remote output saved by ComfyUI. [upscaled.mp4](upscaled.mp4) uses RealESRGAN_x2plus on its decoded frames, then recombines those frames with the generated audio and FPS.

[validation.json](validation.json) contains the measured stream properties, alignment values and SHA-256 hashes. The measured audio lag was 0 ms for the full waveform and the early and late windows. These measurements compare the two saved files; they do not measure whether generated sounds match depicted actions.

The H3 server used two RTX 4090 GPUs, TP2, distributed layerwise offload with eight resident layers, cuDNN attention, and the FL2VA checkpoint partition with LightX2V's 768p v1.0 four-step Turbo adapter. The request used five sigma points, video flow shift 6, audio flow shift 3 and seed 42. ComfyUI ran on a separate RTX 4090. Server versions: vLLM `0.29.0+cu129`, PyTorch `2.13.0+cu129`, NVIDIA driver `570.190`.

The PR's workflow notes contain the startup and validation commands. This evidence branch is separate from the code PR so generated media are not added to the upstream source tree.
